using UnityEngine;

namespace PrototypingPlayground.VRandSplitScreenLocalGame.LobbyToGameplay
{
    public class DumplingPlayer : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
